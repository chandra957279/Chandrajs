// Object Literals

const JsUser = {
    name: "Tuntun",
    age: 22,
    location: "Noida"
}
console.log(JsUser.name);
console.log(`My name is ${JsUser.name} and my age is ${JsUser.age} i'm from ${JsUser.location}`);
